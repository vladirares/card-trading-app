import React from 'react';
import "./TradingPlace.css";

export default function TradingPlace(props){

    // const [listItems,setListItems] = React.useState([])
    
    // React.useEffect(()=>{
    //     getCards(props.username).then((response)=>{
    //         setListItems(response.data);
    //         console.log(props.username)
    //     }).catch((e)=>{
    //         localStorage.clear();
    //     })
    // },[])

    return (
        <div className="trading-place-wrapper">
        {/* <div className="title-wrapper">Your cards:</div>
        <br></br>
    <div className="CardsWrapper">
        {listItems.map(function (card, idCard){
            return (<li className="CardLi" key={idCard}>{<Card params={card}/>}</li>)
        })}
    </div> */}
    </div>)
}